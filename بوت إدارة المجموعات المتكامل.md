# بوت إدارة المجموعات المتكامل v2.0

بوت احترافي متكامل لإدارة مجموعات تيليجرام بميزات خارقة وحماية متقدمة.

## المميزات الرئيسية

### 🛡️ أوامر الإشراف
- **حظر/إلغاء حظر** (`/ban`, `/unban`) - مع دعم الأسباب
- **كتم/إلغاء كتم** (`/mute`, `/unmute`) - كتم دائم أو مؤقت
- **كتم مؤقت** (`/tmute`) - مع تحديد المدة (ثواني/دقائق/ساعات/أيام)
- **طرد** (`/kick`) - طرد مع إمكانية العودة
- **تحذيرات** (`/warn`, `/unwarn`) - حظر تلقائي بعد 3 تحذيرات
- **حذف رسائل** (`/del`, `/purge`) - حذف رسالة أو عدة رسائل
- **تثبيت** (`/pin`, `/unpin`) - تثبيت/إلغاء تثبيت الرسائل

### 📋 نظام القوانين
- `/setrules` - تعيين قوانين المجموعة
- `/rules` - عرض القوانين
- `/clearrules` - حذف القوانين

### 📝 نظام الملاحظات
- `/save` - حفظ ملاحظة بالاسم
- `/get` - استرجاع ملاحظة
- `/notes` - عرض كل الملاحظات
- `/delnote` - حذف ملاحظة

### 🔍 نظام الفلاتر (رد تلقائي)
- `/filter` - إضافة رد تلقائي على كلمة مفتاحية
- `/stop` - حذف فلتر
- `/filters` - عرض الفلاتر النشطة

### 🔒 نظام الأقفال
- `/lock` / `/unlock` - قفل/فتح أنواع الرسائل (صور، فيديو، ملصقات، روابط...)
- `/locks` - عرض الأقفال النشطة
- `/lockall` / `/unlockall` - قفل/فتح الكل
- 16 نوع رسالة قابل للقفل!

### 🛡️ نظام الحماية المتقدم
- **حماية السبام** (`/antispam`) - حذف السبام تلقائياً
- **حماية الفلود** (`/antiflood`) - كتم تلقائي عند الإرسال السريع
- **منع الروابط** (`/antilink`) - حذف الروابط تلقائياً
- **فلتر الكلمات** (`/addbadword`, `/delbadword`, `/badwords`) - حظر كلمات مسيئة
- **وضع الصيانة** (`/maintenance`) - فقط المشرفون يكتبون

### 👤 المعلومات والإحصائيات
- `/info` - معلومات المستخدم التفصيلية
- `/stats` - إحصائيات المجموعة الشاملة
- `/admins` - قائمة المشرفين
- `/log` - سجل الإجراءات

### 📢 مميزات أخرى
- **ترحيب مخصص** (`/setwelcome`) - مع متغيرات {user}, {chat}, {count}
- **نظام البلاغات** - `@admin` أو `/report`
- **نظام الإعلانات** (`/announce`)
- **قناة السجلات** (`/setlog`) - تسجيل كل الإجراءات
- **أزرار تفاعلية** - قائمة أوامر بأزرار
- **كتم مؤقت تلقائي** - فحص كل دقيقة

### 🗄️ قاعدة بيانات SQLite
- تخزين دائم لجميع الإعدادات والبيانات
- لا تفقد البيانات عند إعادة التشغيل
- جداول متعددة منظمة

## المتطلبات
- Python 3.11+
- `python-telegram-bot` 20.8+
- `flask` 3.0.0+

## التشغيل المحلي
1. قم بتثبيت المكتبات: `pip install -r requirements.txt`
2. ضع التوكن في متغير البيئة `TOKEN`
3. قم بتشغيل البوت: `python bot.py`

## النشر على Render
1. اربط المستودع بـ Render
2. أضف متغير البيئة `TOKEN` بقيمة توكن البوت
3. سيتم التشغيل تلقائياً

## الأوامر الكاملة
أرسل `/help` في البوت لرؤية جميع الأوامر المتاحة.
