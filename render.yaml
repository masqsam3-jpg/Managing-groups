services:
  - type: web
    name: group-manager-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TOKEN
        sync: false
      - key: PORT
        value: 8080
    plan: free
